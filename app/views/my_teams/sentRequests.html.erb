<div class="col-lg-6">
	<h2>Requests Sent</h2>
	<div class="table-responsive">
		<table class="table table-hover table-striped">
			<thead>
				<tr>
					<th>From</th>
					<th>To</th>
					<th>Sport</th>
					<th>No. of Players</th>
					<th>Location</th>
				</tr>
			</thead>
			<tbody>
			<%@sentRequests.each do |r|%>
				<tr>
					<%to_id = Team.find(r.request_to)%>
					<%from_id = Team.find(r.request_from)%>
				<td><%=from_id[:team_name]%></td>
				<td><%=to_id[:team_name]%></td>
				<td><%=to_id[:sport_id]%></td>
				<td><%=to_id[:no_of_players]%></td>
				<td><%=to_id[:location]%></td>
				<td><a href=<%=my_teams_deleteRequest_path(id: r.id)%>><button type="button" class="btn btn-danger btn-sm">Delete</button></a></td>
			</tr>
			<%end%>
			</tbody>
		</table>
	</div>
</div>
